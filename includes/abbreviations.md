*[数据格式]: 本模组使用 Minecraft 的 codec 功能将内容序列化为 JSON 或 NBT 或反序列化的格式。
*[Data format]: The format in which this mod uses Minecraft's codec feature to serialize contents as JSON or NBT or deserialize.

*[标签或直接列表]: Minecraft 中的一种特定数据格式（HolderSet），既可以用字符串指定标签名称（以井号开头），也可以直接指定列表。
*[tag or direct list]: A specific data format in Minecraft, which supports both defining tag name with a string (prefixed by hash) and directly defining a list.