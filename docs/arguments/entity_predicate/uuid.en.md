# `uuid`

This [entity predicate](...md) tests entity's uuid. This entity predicate is not defined directly by entity selector arguments. When a uuid is directly used, this predicate will be used.

## Data structure

- `type`: Currently `"enhanced_commands:type"`.
- `uuid`: Uuid.